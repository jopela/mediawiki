(ns mediawiki.raw
  (:require [clj-http.client :as client])

(defn mediawiki-query
  "Performs a raw query against the mediawiki API."
  nil)

(defn generic-query
  "prepare the different urls mediawiki api queries."


